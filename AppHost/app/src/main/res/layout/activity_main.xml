<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/LinearLayout1"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal" >

    <TextView
        android:id="@+id/tvHello"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="我是手Q APP"
        android:textStyle="bold" />
    <ProgressBar
        android:layout_marginTop="15dp"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_below="@id/btInstall"
        android:layout_width="350dp"
        android:layout_height="10dp"
        android:id="@+id/pgDownload"
        android:layout_centerHorizontal="true"
        ></ProgressBar>
    <Button
        android:layout_below="@id/tvHello"
        android:layout_centerHorizontal="true"
        android:id="@+id/btInstall"
        android:layout_width="221dp"
        android:layout_height="48dp"
        android:text="click to install app"
></Button>
    <Button
        android:layout_below="@id/tvHello"
        android:layout_centerHorizontal="true"
        android:id="@+id/btStart"
        android:layout_width="221dp"
        android:layout_height="48dp"
        android:text="click to start app"
        ></Button>

        <TextView
            android:id="@+id/openMarket"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:textAlignment="center"
            android:textColor="#aa5511"
            android:textSize="15dp"
            android:text="打开应用市场测试"
            />
</RelativeLayout>